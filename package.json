{
  "scripts": {
    "prebuild": "del-cli output/*.css",
    "build": "sass input:output --no-source-map && cleancss -O2 'all:on' --batch output/*.css",
    "postbuild": "del-cli output/*[!-][!m][!i][!n].css"
  },
  "devDependencies": {
    "clean-css": "^5.3.1",
    "clean-css-cli": "^5.6.1",
    "del-cli": "^5.0.0",
    "sass": "^1.56.1"
  }
}